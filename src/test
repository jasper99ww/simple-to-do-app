:root {
  --background: #FAFAFA;
  --primary-color: #ECEFF1;
  --secondary-color: #B0BEC5;
  --accent-color: #FF9800;
  --text-color: #37474F; 
  --sidebar-collapsed-width: 0vw; /* 0% szerokości viewport */
  --sidebar-width: 18vw; /* 20% szerokości viewport */
  --mobile-sidebar-collapsed-width: 10vw; /* 10% szerokości viewport dla urządzeń mobilnych */
  --mobile-sidebar-width: 80vw; /* 80% szerokości viewport dla urządzeń mobilnych */
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  font-family: 'Segoe UI', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  font-size: 100%;
  color: var(--text-color);
}

body {
  display: flex;
  min-height: 100vh;
  background-color: var(--background);
  transition: all 0.5s ease;
}

#sidebar {
  display: flex;
  flex-direction: column;
  align-items: stretch;
  height: 100vh;
  gap: 0.625rem;
  width: var(--sidebar-width);
  padding: 0.75rem;
  background-color: var(--primary-color);
  transition: width 0.5s ease, background-color 1.5s ease;
}
/* 
align-items: stretch;
gap: 1.25rem;
height: 100vh;
width: var(--sidebar-width);
padding: 0.75rem;
overflow: hidden;
background-color: var(--primary-color);
transition: width 0.5s ease, background-color 1.5s ease; */


#sidebar h2, #sidebar form, #sidebar ul, #sidebar .search-bar-container {
  padding-left: 0.75rem;
  /* padding-right: 0.75rem; */
  opacity: 1;
  pointer-events: auto;
  transition: opacity 0.5s ease;
}


#sidebar.collapsed {
  width: var(--sidebar-collapsed-width);
}

#sidebar.collapsed h2, #sidebar.collapsed form, #sidebar.collapsed ul {
  opacity: 0;
  pointer-events: none;
}

#sidebar-toggle #sidebar-content {
  padding: 0.75rem;
}

#sidebar-content {
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
  overflow: hidden;
  transition: opacity 0.5s ease;
}

#sidebar.collapsed #sidebar-content {
  opacity: 0;
  pointer-events: none;
}

#toggle-sidebar-btn {
  border: none;
  cursor: pointer;
  background: none;
  padding: 0.65rem;
  border-radius: 50%;
  transition: transform 0.5s ease, background-color 0.3s;
}

#toggle-sidebar-btn svg {
  fill: #455A64;
}


#toggle-sidebar-btn:hover {
  background-color: var(--primary-color);
}

#sidebar #toggle-sidebar-btn {
  transform: rotate(180deg);
}

#main-container {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: 100%;
}

.content, .flex-container-prompt {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  gap: 2rem;
  width: 100%;
}

.content, .flex-container-prompt {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: start;
  margin: 1rem;
  width: 44rem;
}

#current-list-name {
  font-size: 2rem;
  font-weight: 400;
}

#todo-input, #first-list-input {
  padding: 0.75rem 1.25rem;;
  width: 100%;
  background: none;
  border: 2px solid var(--secondary-color);
  border-radius: 1000px;
  font-size: inherit;
  color: var(--text-color);
}

#todo-input:focus, #first-list-input:focus {
  outline: none;
}

#todo-input-form, #first-list-form {
  position: relative;
  width: 100%;
}

#add-btn, #create-first-list-btn {
  position: absolute;
  top: 0;
  right: 0;
  background-color: var(--accent-color);
  height: 100%;
  padding: 0 1.875rem;
  border: none;
  border-radius: 1000px;
  font: inherit;
  font-weight: 600;
  color: var(--background);
  cursor: pointer;
}

#sidebar h2 {
  white-space: nowrap;
  text-overflow: ellipsis;
}

#search-bar-form, #list-form {
  display: flex;
  align-items: center;
  width: 100%;
}

#search-list-input {
  width: 100%;
  border: none;
  outline: none;
  padding: 0.75rem 0.5rem 0.75rem 0.5rem;
  font-size: 1rem;
  background: none;
  color: var(--text-color);
}

#search-list-btn, #create-list-btn{
  padding: 0.3rem;
  margin-right: 0.25rem;
}

#search-list-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  background: none;
  border: none;
  border-radius: 50%;
}

#search-list-btn:hover {
  background: var(--primary-color);
} 

#new-list-input {
  flex-grow: 1;
  border: none;
  outline: none;
  padding: 0.75rem 1.25rem;
  /* margin-right: 5px; */
  font-size: 1rem;
  background-color: rgba(255, 255, 255, 0.2);
  color: var(--text-color);
  min-width: 0;
}

#create-list-btn {
  /* margin-right: 0.675rem; */
  /* margin-right: 0.5rem; */
  /* margin-right: 0.5rem; */
  cursor: pointer;
  background: none;
  border: 0;
  color: inherit;
  font-size: 2rem;
  font-weight: 900;
  transition: 250ms ease-in-out;
}

#create-list-btn:hover {
  color: var(--accent-color);
}

#new-list-input, .search-bar-container #search-bar-form {
  border: 2px solid var(--secondary-color);
  border-radius: 0.75rem;
  background-color: rgba(255, 255, 255, 0.9);
  transition: border 0.3s ease-in-out; /* to color border */
}

#new-list-input:focus, #search-bar-form:focus-within {
  border-color: var(--accent-color);
  background-color: rgba(255, 255, 255, 0.35);
  outline: none;
}

#todo-lists-container{
  list-style: none;
  padding: 0;
  border-radius: 15px;
}


#title {
  margin-top: 50px;
  margin-bottom: 50px;
  font-size: 3rem;
  font-weight: 800;
  text-transform: uppercase;
  text-align: center;
  color: var(--accent-color);
  /* margin-bottom: 20px; Dostosuj odstęp pod tytułem */
}

.todo {
  background-color: var(--primary-color);
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.15);
  margin-bottom: 10px;
  padding: 0 12px;
}

.list-item {
  background-color: var(--background);
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.15);
  display: flex;
  align-items: center;
  padding: 0 12px;
  border-radius: 15px;
  max-height: 5rem;
  /* overflow: hidden; Ukryj nadmiarowy tekst */
}

#todo-lists-container li:not(:last-child) {
  margin-bottom: 10px;
}

.drag-btn.dragging {
  cursor: grabbing;
}

.sortable-chosen.sortable-ghost {
	opacity: 0;
}

.sortable-ghost, .sortable-chosen {
  cursor: grabbing;
}

.sortable-fallback {
	opacity: 1 !important;
}


.list-item.active{
  border: 2px solid var(--accent-color);
}

.list-item:hover {
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
}

.todo, .list-item {
  display: flex;
  align-items: center;
  border-radius: 15px;
}


.icon-container {
  display: flex;
}

.edit-list-btn, .delete-list-btn {
  padding: 0px;
  background: none;
  border: none;
  cursor: pointer;
}

.drag-btn {
  padding: 0px;
  background: none;
  border: none;
  cursor: grab;
}

.grabbing * {
  cursor: grabbing !important;
}

#todo-list{
  width: 100%;
}

.todo-text {
  overflow: hidden;
  overflow-wrap: break-word;
}


.todo-list-text{
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.todo-text, .todo-list-text{
  padding: 14px 7px;
  padding-right: 0;
  flex-grow: 1;
  transition: 0.5s ease;
  resize: none;
  background: none;
  border: none;

}

.todo-text:focus, .todo-list-text:focus {
  outline: none;
  margin: 10px;
  box-shadow: 0 0 8px var(--secondary-color);
  transition: box-shadow 0.3s ease-in-out;
}

.edit-btn, .delete-btn {
  padding: 3px;
  background: none;
  border: none;
  cursor: pointer;
  cursor: pointer;
}

.edit-btn, .delete-btn svg, .edit-list-btn, .delete-list-btn svg, .drag-btn svg {
  fill: #455A64;
  transition: 250ms ease-in-out;
}

.delete-btn:hover svg, .delete-list-btn:hover svg {
  fill: #D32F2F;
}

.edit-btn:hover, .edit-list-btn:hover, .drag-btn:hover svg {
  fill: var(--accent-color)
}

.custom-checkbox{
  border: 2px solid var(--accent-color);
  border-radius: 50%;
  min-width: 20px;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-shrink: 0;
  transition: 200ms ease;
  cursor: pointer;
}
input[type="checkbox"]:checked ~ .custom-checkbox{
  background-color: var(--accent-color);
}
input[type="checkbox"]:checked ~ .custom-checkbox svg{
  fill: var(--primary-color)
}
input[type="checkbox"]:checked ~ .todo-text, input[type="checkbox"]:checked ~ .todo-list-text{
  text-decoration: line-through;
  color: var(--secondary-color);
}
input[type="checkbox"]{
  display: none;
}

/* Style dla urządzeń o szerokości ekranu do 768px */
@media (max-width: 768px) {

  #main-container.active-overlay::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, .4);
    z-index: 9999; /* Ustawienie z-index na wartość mniejszą niż zawartość, aby overlay był w tle */
  }

  #sidebar:not(.collapsed) + #main-container {
    position: relative; /* Niezbędne, aby pseudo-element był względem tego kontenera */
  }
  
  html {
    font-size: 14pt;
  }
  
  #sidebar {
    width: var(--mobile-sidebar-width);
  }

  #main-container {
    width: 100%;
    padding: 0 1rem;
  }

  #sidebar + #main-container {
    background-color: rgba(0, 0, 0, .4);
    /* filter: brightness(0.5); */
    /* background-color: rgba(0, 0, 0, 0.4); */
  }

  #sidebar.collapsed {
    width: var(--mobile-sidebar-collapsed-width);
  }

  #sidebar.collapsed + #main-container {
    background-color: rgba(0, 0, 0, 0);
    /* background-color: rgba(0, 0, 0, .4); */
    /* filter: brightness(1); */
  }

  #add-btn {
    position: static;
    width: 100%;
    margin-top: 10px;
    padding: 15px;
    height: auto;
  }

  #todo-input-form, #first-list-form {
    /* display: flex;
    flex-direction: column; */
    /* width: 100%; */
  }

  .content, .flex-container-prompt {
    width: 100%;
    margin: 0;
  }

}



<nav id="sidebar">
    <div id="sidebar-toggle">
      <button id="toggle-sidebar-btn">
        <svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="#5f6368">
          <path d="M120-240v-80h720v80H120Zm0-200v-80h720v80H120Zm0-200v-80h720v80H120Z"/>
        </svg>
      </button>
    </div>
    <div id="sidebar-content">
      <h2>My lists</h2>
      <div class="search-bar-container">
        <form id="search-bar-form" role="search">
          <input type="text" id="search-list-input" placeholder="Search lists...">
          <button type="submit" id="search-list-btn">
            <img src="src/search.svg" alt="search button">
          </button>
        </form>
      </div>
      <form id="list-form">
        <input type="text" id="new-list-input" placeholder="New list name"/>
        <button id="create-list-btn">+</button>
      </form>
      <ul id="todo-lists-container"></ul>
    </div>
</nav>


  <div id="main-container">
    <h1 id="title">Todo App</h1>

    <div class="content">
      <p id="current-list-name">Current List Name</p>
      <form id="todo-input-form">
        <input type="text" id="todo-input" placeholder="Add a new task and press enter" autocomplete="off">
        <button id="add-btn">ADD</button>
      </form>
      <ul id="todo-list"></ul>
    </div>
  
    <div class="flex-container-prompt">
        <h2>Welcome to Todo App!</h2>
        <h3>You don't have any lists yet. Create your first list now:</h3>
        <form id="first-list-form">
          <input type="text" id="first-list-input" placeholder="Enter a name for your first list" autocomplete="off">
          <button id="create-first-list-btn">Create List</button>
        </form>
    </div>

  </div>